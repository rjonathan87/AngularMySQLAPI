cd server 
npm init --yes

npm i express morgan promise-mysql cors

>> src/index.ts

tsconfig.json
tsc --init
    "target": "es6",
    "outDir": "./build"

package.json
    
    "scripts": {
        "build": "tsc -w"
    },

    npm run build

npm i nodemon -D  

/* Server Settings */
npm i @types/express -D

/* Install types */
npm i @types/morgan @types/cors 



